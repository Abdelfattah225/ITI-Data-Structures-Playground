#include <stdio.h>


// arr : 
void ctor(arr,size){
    int temp = arr[n];
    int index =  size;
    while( index > 1 &&temp > arr[index/2]) // is 40 > parent{}
    {
        arr[index] = arr[index/2]
        i = i/2
    }
    arr[i]  = temp;
}